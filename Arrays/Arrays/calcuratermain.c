#include <stdio.h> // 얘가 만든 헤더
#include "Calculater.h" // 내가 만든 헤더
int main()
{
	int x = 3, y = 4, result;
	count++;




	result = addguard(x, y);
	printf("%d\n", count);
	printf("%d\n", result);



	return 0;
}

