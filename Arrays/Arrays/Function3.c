#include <stdio.h>
#include <math.h>
int myAbs(int x)
{
	if (x < 0)
	{
		return -x;
	}
	else
	{
		return x;
	}

}

int main_gfdghxdg() {

	int value, value3;
	value = abs(-3);

	value3 = myAbs(-23);
	printf("%d", value3);










































	return 0;
}